# 衛星遙測影像 AI 物件偵測系統

**專案總結報告** ｜ 開發期間：2025/08 – 2025/09（21 天）

---

## 一、專案概述

本專案建構了一條**端到端的遙測視覺（Remote Sensing Computer Vision）處理鏈**，從歐洲太空總署 Sentinel-2 衛星的原始多光譜資料出發，經過影像前處理、資料標註、模型訓練與切片推論，最終將 AI 偵測到的像素座標透過仿射變換還原為真實世界的 WGS84 經緯度。

不同於一般電腦視覺專案在「模型畫出框」時即告結束，本專案的產出是**可直接定位到地圖上的地理座標**，具備實際 GIS 應用價值。

**目前狀態**：AI 與地理空間核心鏈路已完成並可運作，Web 系統層（FastAPI / React）尚未開發。

---

## 二、系統架構

```
[ 資料源 ]
   └── Sentinel-2 多光譜衛星影像（10m/pixel, 16-bit）
              │
              ▼
[ 影像前處理 ]
   ├── SNAP：波段融合 + 重新取樣（Resampling）→ GeoTIFF
   ├── Rasterio：16-bit → 8-bit 壓縮、波段轉置
   ├── 動態直方圖拉伸（Percentile Stretch, 分通道 + 排除 NoData）
   └── CLAHE：LAB 色彩空間局部對比度增強
              │
              ▼
[ 資料集工程 ]
   ├── Shapely：切片視窗與標註多邊形布林交集
   ├── Polygon 座標重映射（全局 → 局部正規化）
   └── 正負樣本採樣策略（背景切片保留率 15%）
              │
              ▼
[ AI 模型 ]
   ├── YOLOv8-OBB：旋轉邊界框偵測（x, y, w, h, θ）
   └── SAHI：切片輔助推論（保留原始解析度）
              │
              ▼
[ 後處理（自研） ]
   └── PCA 主軸擬合 + 穩健離群剔除 → 碎框幾何縫合
              │
              ▼
[ 地理空間轉換 ]
   ├── Affine Transform：像素座標 → 投影座標
   └── pyproj：動態 CRS → EPSG:4326（WGS84 經緯度）
              │
              ▼
[ 輸出 ] 可直接導航的 GPS 座標 / Google Maps 連結
```

---

## 三、量化成果

| 指標 | 數值 | 說明 |
|---|---|---|
| 訓練速度 | 50 epochs / 21 秒 | RTX 5070 Ti，Blackwell sm_120 架構 |
| 推論速度 | 2.6 ms / 張 | 約 385 FPS，具備處理 TB 級每日資料流的吞吐量 |
| 模型精準度 | Precision 0.854 | YOLOv8-OBB，整圖驗證集 |
| 偵測完整度提升 | conf 0.27 → 0.56 | 真實 GeoTIFF 上，跑道從「半截」到「全線覆蓋」 |
| 後處理有效性 | 6 個碎框 → 4 個框 | 自研幾何合併，3 個碎片縫合為一條完整跑道 |
| 資料集擴增 | 18 張原圖 → 96 張切片 | 含 93 個重映射後的多邊形碎片標註 |

---

## 四、關鍵技術決策

以下五個決策，每一個都是「觀察到問題 → 分析根因 → 提出方案 → 量化驗證」的完整循環。

### 決策 1：資料源取捨與偵測目標的重新定義

**問題**：原訂使用國家太空中心（TASA）影像，但需申請流程；改用開源 Sentinel-2 後，發現其空間解析度為 10m/pixel。

**分析**：一架客機長度約 30–45m，在影像中僅佔 3–4 個像素，人眼無法辨識，更無法進行可靠的人工標註。若強行標註，標註誤差本身就會超過目標物尺寸。

**決策**：**主動將偵測目標從「飛機」調整為「機場跑道」**，讓目標物尺度與感測器解析度相匹配。同時明確記錄此為資料源的物理上限——若未來要偵測更小目標，必須升級至商用高解析度衛星，而非調整演算法。

> 這個決策的價值在於：辨識出這是**資料層的硬限制而非模型問題**，避免了在錯誤方向上投入訓練資源。

### 決策 2：從水平框（HBB）升級為旋轉框（OBB）

**問題**：初版模型假陽性率極高，且轉換出的中心點座標嚴重偏移。

**分析**：機場跑道為配合當地盛行風向而建，在正北朝上的衛星圖中幾乎都是傾斜的。當一個長寬比 10:1 的物體以 45° 傾斜時，包覆它的最小水平框內部**超過 80% 的面積是背景**（草地、農田、民宅）。模型因此無法區分「灰色柏油跑道」與「旁邊的綠色草地」何者才是特徵——這是假陽性的幾何根因。

**方案**：改用 YOLOv8-OBB，引入角度變數 θ，讓邊界框平行於跑道走向。底層採用 ProbIoU（將旋轉矩形視為二維高斯分佈，以巴氏距離評估重疊率）計算損失。

**結果**：Precision 達 0.854，背景雜訊被有效剔除，中心點定位偏移問題同步解決。

### 決策 3：巨幅影像推論的三層問題拆解

真實 GeoTIFF 推論初期輸出全空白，逐層定位出三個獨立問題：

| 層級 | 問題 | 解法 |
|---|---|---|
| I/O 層 | OpenCV 無法讀取 16-bit 多波段 GeoTIFF | Rasterio 抽取 RGB → 轉置 (3,H,W)→(H,W,3) → 位元壓縮至 uint8 |
| 尺度層 | 整圖 resize 至 640×640，跑道被壓成不足 1 像素 | 導入 SAHI 切片推論，維持原始解析度 |
| 影像層 | 雲層反光數值頂天，壓縮後其他地物對比度趨近於零 | CLAHE 局部直方圖均衡化 |

**CLAHE 的實作細節值得一提**：不能直接在 RGB 通道操作，那會破壞整張圖的色彩協調性。正確做法是轉入 **LAB 色彩空間**，將亮度（L）與色彩（A, B）分離，僅對 L 通道套用 8×8 網格的局部拉伸，再縫合回 RGB。

### 決策 4：實測證明「感受野領域偏移」是斷崖而非隱憂 ⭐

**問題**：SAHI 切片推論後，一條完整跑道只被認出右上半截，左下段被當成雜訊濾除。

**分析**：這是典型的 **Receptive Field Domain Gap**。訓練時模型看到的是被 resize 的完整機場，學到的是「跑道 + 航廈 + 停機坪」的全局組合特徵；推論時 SAHI 切下的 640×640 真實像素中，可能只有「一條沒有盡頭的灰色柏油路」，完全缺乏航廈與邊界特徵。模型不敢給出高信心度。

**方案**：實施 **Patch-based Training**——「推論時怎麼切，訓練時就怎麼切」。使用 Shapely 對訓練集執行布林交集切片，並重映射被切斷的多邊形標註。

**驗證方法**：設計 **2×2 交叉驗證**，將兩個模型分別丟到兩個驗證集上互測。

| 模型 | 驗證集 | Precision | Recall | mAP50 | mAP50-95 |
|---|---|---|---|---|---|
| baseline（整圖訓練） | 原始整圖 valid | 0.853 | 0.500 | 0.495 | 0.346 |
| baseline（整圖訓練） | 切片 valid | **0.000** | **0.000** | **0.000** | **0.000** |
| tiled（切片訓練） | 原始整圖 valid | 0.189 | 0.500 | 0.099 | 0.069 |
| tiled（切片訓練） | 切片 valid | 0.541 | 0.250 | 0.205 | 0.131 |

**第二列的三個 0.000 是本專案最重要的實測結論**：在整圖上 Precision 高達 0.853、表現優異的 baseline 模型，換到切片驗證集上是**完全歸零**——不是「表現變差」，是徹底失明。

第三列同樣關鍵：切片模型回到整圖上 mAP50 僅剩 0.099，它把全局視野忘掉了。兩個模型各自只在自己的領域內有效，構成一組非常乾淨的 Domain Gap 對照實驗。

**真實影像驗證**（同一張 GeoTIFF、同一套 CLAHE 前處理、同樣 0.25 信心門檻，僅切換權重與切片尺寸）：

```
[baseline_slice640] 偵測到 1 個目標  conf = [0.266]
[tiled_slice320]    偵測到 6 個目標  conf = [0.559, 0.354, 0.325, 0.316, 0.295, 0.273]
```

跑道覆蓋範圍從「右上半截」延伸至整條，最高信心度從 0.27 提升至 0.56。

> 附帶的資料集工程細節：Shapely 切片過程踩到三個陷阱——`intersection()` 回傳型別不固定（Polygon / MultiPolygon / GeometryCollection）、手繪標註普遍自相交需以 `buffer(0)` 修復、面積過小的碎片標註是純噪音（設絕對 64 px² 與相對 5% 雙重門檻濾除）。並實施正負樣本採樣策略，避免 66 張純背景切片以海量負樣本淹沒損失函數。

### 決策 5：自研幾何後處理，解決套件無法處理的碎框問題 ⭐

**問題**：切片訓練解決了漏檢，但代價是一條跑道被拆成 6 個獨立的歪斜框——模型學會了「認出跑道的一小段」，卻不知道這些片段屬於同一條跑道。

**分析**：**SAHI 內建的 NMS / NMM / GREEDYNMM 全部失效**，因為這三種演算法一律以「重疊率」（IoU / IoS）為合併判準，而沿著跑道線性排開的碎片，彼此重疊率是 0。這不是參數調校能解決的，是判準本身的維度不對。

**方案**：跳脫重疊率思維，改以**幾何拓撲**作為合併判準，自行實作 `inference/merge_fragments.py`：

1. **間距分群**：以 union-find 將邊界距離 ≤ 70px 的碎片連成連通分量
2. **PCA 主軸擬合**：對群內所有角點做奇異值分解，取第一主成分為跑道主軸
3. **穩健離群剔除**：計算各碎片質心對主軸的垂直偏移，超過 3× 中位數者剔除後重新擬合（最多迭代 5 次）——跑道是細長物件，真正屬於它的碎片必然共線，偏離主軸的多半是市區或海岸線誤判，此步驟同時具備假陽性過濾效果
4. **OBB 重建**：沿主軸取投影範圍，重建貼齊跑道方向的旋轉框

**結果**：6 個碎框 → 4 個框，其中 3 個碎片成功縫合為一條貫穿全跑道的完整 OBB（conf 0.56），且被剔除的離群碎片保留原框輸出，不會憑空消失。

> 這一段是本專案技術含量最高的部分：**當現成套件的三種後處理演算法全部失效時，能夠分析出失效的根本原因（判準維度錯誤），並基於問題的幾何本質自行設計替代方案。**

---

## 五、地理空間轉換

AI 輸出的像素座標，透過線性代數的仿射變換還原為真實世界座標：

- **平移參數 (c, f)**：影像左上角在真實地圖上的絕對座標，作為定位基準點
- **解析度參數 (a, e)**：單一像素的實際地面距離。其中 e 恆為負值——螢幕 Y 軸向下遞增，而地球緯度向下遞減，需以負號反轉方向
- **旋轉錯切參數 (b, d)**：影像相對正北的偏移角度，經正射校正的影像通常為 0

實作上以 Rasterio 讀取 GeoTIFF Metadata 中的六參數矩陣，並透過 pyproj 動態偵測來源 CRS（不論下載的影像屬於哪個 UTM Zone），統一轉換至 Google Maps 與 GPS 通用的 EPSG:4326。

---

## 六、工程實踐

專案採模組化架構，各階段職責分離：

```
preprocessing/   crop_images.py       影像切片
                 download_dataset.py  Roboflow API 資料集拉取
                 tile_dataset.py      Shapely 幾何切片與標註重映射
training/        train.py             baseline HBB 訓練
                 train_v2/v3.py       超參數與資料增強實驗
                 train_obb.py         OBB 旋轉框訓練
                 train_tiled.py       切片資料集訓練
inference/       geo_utils.py         仿射變換與 CRS 轉換
                 merge_fragments.py   自研碎框幾何合併
Tests/           7 支驗證腳本，涵蓋 TIFF 讀取、OBB 推論、SAHI 切片、碎框合併
```

其他工程細節：

- **環境容器化**：Docker Compose 部署 PostGIS，環境變數隔離敏感資訊
- **自動化防錯**：推論腳本以 `st_mtime` 動態搜尋最新權重，避免 YOLO 自動遞增資料夾導致的路徑錯誤
- **視覺化驗證**：切片標註不僅看統計數字，另寫腳本將重映射後的 Polygon（綠）與 `cv2.minAreaRect()` 計算的 OBB（紅）疊圖人工確認對齊
- **硬體適配**：RTX 5070 Ti 採用全新 Blackwell 架構（sm_120），標準 PyTorch CUDA 12.4 僅支援至 sm_90，改裝 nightly build 後方能釋放算力

---

## 七、現況限制

誠實列出目前的瓶頸，這些也是後續投入的優先順序依據：

| 限制 | 影響 | 性質 |
|---|---|---|
| 資料集僅 18 張原圖 | 模型泛化能力不足，各項指標波動大 | **資料層瓶頸**，非演算法可解 |
| Sentinel-2 僅 10m/pixel | 無法偵測小於 30m 的目標物 | **感測器物理上限**，需商用衛星 |
| 精確度與召回率的取捨 | 切片模型 mAP50-95 僅 0.131，框的幾何精度退化 | 演算法層，可透過混合資料集改善 |
| 尚無 Web 系統層 | 目前為 CLI 腳本，無視覺化介面 | 已規劃，尚未開發 |

---

## 八、後續規劃

**近期（演算法層）**
1. **混合資料集訓練**：整圖與切片資料同時投入，讓模型兼具全局視野與局部細節，解決當前「兩個模型各自只在自己領域有效」的問題
2. **幾何合併參數調優**：目前間距門檻 70px 為經驗值，可依目標物類型自適應

**中期（資料層）**
3. **擴充資料集規模**：現有 18 張原圖是所有指標的根本天花板
4. **評估商用高解析度影像**：若需偵測船舶、車輛等目標，此為必要條件

**長期（系統層）**
5. **後端 API**：FastAPI 封裝模型推論與 PostGIS 空間查詢
6. **前端視覺化**：React + Leaflet.js / OpenStreetMap，將偵測結果以 GeoJSON 呈現於互動地圖，並建置 Data Board

---

## 九、技術棧

| 領域 | 技術 |
|---|---|
| 深度學習 | PyTorch, Ultralytics YOLOv8-OBB, SAHI |
| 地理空間 | Rasterio, pyproj, Shapely, GDAL, PostGIS, SNAP |
| 影像處理 | OpenCV, NumPy, CLAHE, Percentile Stretch |
| 資料工程 | Roboflow API, python-dotenv |
| 基礎設施 | Docker Compose, PostgreSQL |
| 規劃中 | FastAPI, React, Leaflet.js |

---

## 十、結語

這 21 天的開發過程中，真正的技術難點都不在「訓練一個模型」，而在於**辨識問題發生在哪一層**：

- 飛機偵測失敗，是**資料源的物理限制**，不是模型不夠強
- 假陽性爆炸，是**邊界框的幾何形式錯誤**，不是超參數沒調好
- 推論輸出空白，是 **I/O、尺度、對比度三個獨立問題疊加**，需逐層拆解
- 跑道被切半，是**訓練與推論的領域偏移**，用 2×2 交叉驗證量化出 0.000 的斷崖
- 碎框無法合併，是**現成演算法的判準維度不對**，必須自行設計幾何解法

每一個結論都經過實測數據驗證，而非推測。這也是本專案最希望呈現的能力：**面對一個沒有標準答案的問題時，如何定位根因、設計驗證、並誠實面對方案的代價。**
